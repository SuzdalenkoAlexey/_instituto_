UBUNTU SERVER:
SIN PROBAR: vboxuser changeme
    sudo apt update
    sudo apt upgrade

    sudo apt install mysql-server

    sudo mysql_secure_installation

    sudo mysql -u root -p

    CREATE DATABASE mi_base_de_datos;

    CREATE USER 'alexey'@'localhost' IDENTIFIED BY '123456';
    GRANT ALL PRIVILEGES ON *.* TO 'alexey'@'localhost';

    GRANT ALL PRIVILEGES ON mi_base_de_datos.* TO 'alexey'@'localhost';
    FLUSH PRIVILEGES;

    USE mi_base_de_datos;

    CREATE TABLE clientes (
        id INT AUTO_INCREMENT PRIMARY KEY,
        nombre VARCHAR(100),
        direccion VARCHAR(255),
        telefono VARCHAR(15),
        email VARCHAR(100)
    );
    INSERT INTO clientes (nombre, direccion, telefono, email) VALUES ('Juan Perez', '123 Calle Principal', '555-1234', 'juanperez@example.com');
    SELECT * FROM clientes;
    EXIT;
    sudo systemctl status mysql

PROBANDO:
